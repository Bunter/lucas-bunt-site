"use client";
import {useState} from "react";
export function OwnerLogin(){const [busy,setBusy]=useState(false),[error,setError]=useState("");return <main id="main-content" className="section-page owner-login"><p className="eyebrow">JUST FOR LUCAS</p><h1>Your creative<br/><em>home base.</em></h1><p className="section-lead">Write something, share a photo, or keep a recipe worth making again.</p><form className="login-card" onSubmit={async e=>{e.preventDefault();setBusy(true);setError("");const form=new FormData(e.currentTarget);try{const r=await fetch("/api/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username:form.get("username"),password:form.get("password")})});if(!r.ok){const data=await r.json() as {error:string};throw new Error(data.error);}window.location.assign("/admin");}catch(e){setError((e as Error).message||"Couldn't sign in. Please try again.");setBusy(false);}}}><h2>Welcome back.</h2><p>Sign in to your owner workspace.</p><label>Username<input name="username" autoComplete="username" required maxLength={100} autoCapitalize="none" spellCheck={false}/></label><label>Password<input name="password" type="password" autoComplete="current-password" required maxLength={256}/></label>{error&&<p className="owner-alert" role="alert">{error}</p>}<button className="owner-button" disabled={busy}>{busy?"Signing in…":"Sign in"}</button></form></main>;}
